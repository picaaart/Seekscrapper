name: Seek Scraper

on:
  schedule:
    # Exécute toutes les 2 heures
    - cron: '0 */2 * * *'
  workflow_dispatch:  # Permet de lancer manuellement

jobs:
  scrape:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3

    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.9'

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt
        python -m playwright install chromium

    - name: Run scraper
      run: |
        cd /home/runner/work/seek-scraper/seek-scraper
        python3 seek_scraper.py

    - name: Commit and push changes
      run: |
        git config --local user.email "action@github.com"
        git config --local user.name "GitHub Action"
        git add data/labourer_jobs_queensland.csv
        git diff --quiet && git diff --staged --quiet || git commit -m "🔄 Auto scrape: $(date -u +'%Y-%m-%d %H:%M:%S UTC')"
        git push
