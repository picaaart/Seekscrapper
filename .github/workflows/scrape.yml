name: Seek Scraper - Optimized Multi Category

on:
  schedule:
    - cron: '0 */2 * * *'
  workflow_dispatch:

jobs:
  scrape:
    runs-on: ubuntu-latest
    timeout-minutes: 30

    steps:
    - uses: actions/checkout@v3

    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.9'

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt
        python -m playwright install chromium

    - name: Run optimized multi-category scraper
      run: |
        cd ${{ github.workspace }}
        python3 optimized_scraper.py

    - name: Commit and push changes
      run: |
        git config --local user.email "action@github.com"
        git config --local user.name "GitHub Action"
        git add data/jobs_australia_current.csv data/jobs_australia_archive.csv data/scrape_cache.json data/company_stats.csv data/location_stats.csv data/category_stats.csv data/monthly_trends.json data/monthly_trends.csv
        git diff --quiet && git diff --staged --quiet || (git commit -m "⚡ Scrape + analytics + trends: $(date -u +'%Y-%m-%d %H:%M:%S UTC')" && git push)
