name: Seek Scraper - Multi Category

on:
  schedule:
    - cron: '0 */2 * * *'
  workflow_dispatch:

jobs:
  scrape:
    runs-on: ubuntu-latest
    timeout-minutes: 120

    steps:
    - uses: actions/checkout@v3

    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.9'

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt
        python -m playwright install chromium

    - name: Run multi-category scraper
      run: |
        cd ${{ github.workspace }}
        python3 seek_scraper.py

    - name: Commit and push changes
      run: |
        git config --local user.email "action@github.com"
        git config --local user.name "GitHub Action"
        git add data/jobs_australia_current.csv data/jobs_australia_archive.csv
        git diff --quiet && git diff --staged --quiet || (git commit -m "🔄 Auto scrape multi-category: $(date -u +'%Y-%m-%d %H:%M:%S UTC')" && git push)
